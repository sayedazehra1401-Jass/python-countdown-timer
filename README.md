# python-countdown-timer
A simple Python countdown timer mini project
# Countdown Timer ⏳

A simple Python-based countdown timer that runs in the terminal.

## Features
- Accepts user input for countdown duration (in seconds).
- Displays time in **MM:SS** format.
- Alerts with "Time's up!" when finished.

## Usage
1. Clone the repository:
   ```bash
   git clone https://github.com/sayedazehra1401-Jass/countdown-timer.git
   cd countdown-timer
